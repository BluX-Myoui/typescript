# Laboratorio 1 — Inicialización y lógica pura

Funciones estadísticas tipadas en `src/math-utils.ts` con retorno `number | null` para arrays vacíos.

Ejecución: `npm run dev` · Compilación: `npm run build`
